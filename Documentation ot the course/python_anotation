                Modo interativo:

Iniciar o modo interativo do Python:

 - chamando 'python' no terminal, ou executando com a flag -i(python -i app.py)
            
                Função Dir:

Sem argumentos, retorna a lista de nomes no escopo local atual. Com um argumento, retorna uma
lista de atributos válidos para o objeto. Exemplo:

dir()
dir(100)
 
